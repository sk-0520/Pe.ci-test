<ResourceDictionary
	x:Class="ContentTypeTextNet.Pe.PeMain.View.MainWorker"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
	xmlns:Taskbar="http://www.hardcodet.net/taskbar"
	xmlns:AV="clr-namespace:ContentTypeTextNet.Pe.PeMain.ViewModel"
	xmlns:AA="clr-namespace:ContentTypeTextNet.Pe.PeMain.View.Parts.Attached"
	xmlns:SC="clr-namespace:ContentTypeTextNet.Library.SharedLibrary.View.Converter;assembly=SharedLibrary"
>
	<ResourceDictionary.MergedDictionaries>
		<ResourceDictionary Source="/View/Parts/Resource/MenuItemDictionary.xaml"/>
	</ResourceDictionary.MergedDictionaries>
	<ContextMenu x:Shared="false" x:Key="ContextMenu" IsOpen="{Binding IsContextMenuOpen}">
		<i:Interaction.Triggers>
			<i:EventTrigger EventName="Opened">
				<i:InvokeCommandAction Command="{Binding OpenContextMenuCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
			</i:EventTrigger>
		</i:Interaction.Triggers>
		<ContextMenu.Resources>
			<SC:LogicalNotConverter x:Key="LogicalNotConverter"/>
			<SC:IsNotEmptyCollection x:Key="IsNotEmptyCollection" />

			<Style x:Key="NoteMenuItemStyle" TargetType="MenuItem" BasedOn="{StaticResource IMenuItem}">
				<Setter Property="Command" Value="{Binding NoteMenuSelectedCommand}" />
			</Style>

			<Style x:Key="WindowMenuItemStyle" TargetType="MenuItem" BasedOn="{StaticResource IMenuItem}">
				<Setter Property="Command" Value="{Binding WindowMenuSelectedCommand}" />
			</Style>
		</ContextMenu.Resources>
		<MenuItem AA:Language.Word="tasktray/setting" Header="setting" Command="{Binding ShowSettingWindowCommand}" />
		<Separator />
		<MenuItem AA:Language.Word="tasktray/toolbar" Header="toolbar" ItemsSource="{Binding LauncherToolbars}">
			<MenuItem.ItemContainerStyle>
				<Style TargetType="MenuItem" BasedOn="{StaticResource IMenuItem}">
					<!--<Setter Property="IsChecked" Value="{Binding IsVisible, Mode=OneWay}"/>-->
					<Setter Property="Command" Value="{Binding ChangeVisibleCommand}" />
					<Setter Property="CommandParameter" Value="{Binding IsVisible, Converter={StaticResource LogicalNotConverter}}" />
				</Style>
			</MenuItem.ItemContainerStyle>
			<!--<MenuItem.ItemTemplate>
				<DataTemplate>
					<StackPanel Orientation="Horizontal">
						<Image Source="{Binding ScreenPosition}" />
						<TextBlock Text="{Binding ScreenName}" />
					</StackPanel>
				</DataTemplate>
			</MenuItem.ItemTemplate>-->
		</MenuItem>
		<MenuItem AA:Language.Word="tasktray/note"    Header="note">
			<MenuItem AA:Language.Word="note/create" Header="create" Command="{Binding CreateNoteItemCommand}" InputGestureText="{Binding CreateNoteHotKey}" />
			<MenuItem Header="compact" Command="{Binding CompactNoteItemsCommand}" InputGestureText="{Binding CompactNoteItemsHotKey}" />
			<MenuItem Header="hide" Command="{Binding HideNoteItemsCommand}"  InputGestureText="{Binding HideNoteItemsHotKey}" />
			<Separator />
			<MenuItem Header="front" Command="{Binding FrontNoteItemsCommand}" InputGestureText="{Binding FrontNoteItemsHotKey}"/>
			<Separator />
			<MenuItem Header="show" ItemsSource="{Binding NoteShowItems}" ItemContainerStyle="{StaticResource NoteMenuItemStyle}" IsEnabled="{Binding ItemsSource,RelativeSource={RelativeSource Self}, Converter={StaticResource IsNotEmptyCollection}}"/>
			<MenuItem Header="hidden" ItemsSource="{Binding NoteHiddenItems}" ItemContainerStyle="{StaticResource NoteMenuItemStyle}" IsEnabled="{Binding ItemsSource,RelativeSource={RelativeSource Self}, Converter={StaticResource IsNotEmptyCollection}}"/>
		</MenuItem>
		<MenuItem AA:Language.Word="tasktray/template" Header="template" Command="{Binding SwitchTemplateWindowCommand}" IsChecked="{Binding Template.IsVisible, Mode=OneWay}" InputGestureText="{Binding SwitchTemplateWindowHotKey}" />
		<MenuItem AA:Language.Word="tasktray/command" Header="command" Command="{Binding ShowCommandWindowCommand}" InputGestureText="{Binding ShowCommandWindowHotKey}" />
		<MenuItem AA:Language.Word="tasktray/log"     Header="log" Command="{Binding SwitchLoggingWindowCommand}" IsChecked="{Binding Logging.IsVisible, Mode=OneWay}" />
		<Separator />
		<MenuItem AA:Language.Word="tasktray/system-env" Header="system-env">
			<i:Interaction.Triggers>
				<i:EventTrigger EventName="SubmenuOpened">
					<i:InvokeCommandAction Command="{Binding OpenSystemEnvironmentMenuCommand}" />
				</i:EventTrigger>
			</i:Interaction.Triggers>
			<MenuItem Header="hide file" Command="{Binding SwitchShellHideFileCommand}" IsChecked="{Binding IsVisibledShellHideFile, Mode=OneWay}"  InputGestureText="{Binding SwitchShellHideFileHotKey}"/>
			<MenuItem Header="extension" Command="{Binding SwitchShellExtensionCommand}" IsChecked="{Binding IsVisibledShellExtension, Mode=OneWay}"  InputGestureText="{Binding SwitchShellExtensionHotKey}"/>
			<Separator />
			<MenuItem Header="clipboard" Command="{Binding SwitchClipboardWindowCommand}" IsChecked="{Binding Clipboard.IsVisible}" InputGestureText="{Binding SwitchClipboardWindowHotKey}" />
			<Separator />
			<MenuItem Header="window - save" Command="{Binding SaveTemporaryWindow}" />
			<MenuItem Header="window - load" Command="{Binding LoadTemporaryWindow}" />
			<MenuItem Header="window - timer" ItemsSource="{Binding WindowTimerItems}" ItemContainerStyle="{StaticResource ResourceKey=WindowMenuItemStyle}" IsEnabled="{Binding ItemsSource,RelativeSource={RelativeSource Self}, Converter={StaticResource IsNotEmptyCollection}}" />
			<MenuItem Header="window - system" ItemsSource="{Binding WindowSystemItems}" ItemContainerStyle="{StaticResource ResourceKey=WindowMenuItemStyle}" IsEnabled="{Binding ItemsSource,RelativeSource={RelativeSource Self}, Converter={StaticResource IsNotEmptyCollection}}" />
		</MenuItem>
		<Separator />
		<MenuItem AA:Language.Word="tasktray/about" Header="about" />
		<MenuItem AA:Language.Word="tasktray/exit" Header="exit" Command="{Binding ExitApplicationCommand}" />
	</ContextMenu>

	<Taskbar:TaskbarIcon 
		x:Key="root"
		Tag="{Binding DataContext,RelativeSource={RelativeSource Mode=Self}, Mode=OneWay}"
		IconSource="{Binding ApplicationIcon, Mode=OneWay}"
		ToolTipText="Double-click for window, right-click for menu"
		ContextMenu="{StaticResource ResourceKey=ContextMenu}"
	/>

	<!--<Taskbar:TaskbarIcon 
		x:Key="root"
		x:Name="root"
		Tag="{Binding DataContext,RelativeSource={RelativeSource Mode=Self}, Mode=OneWay}"
		IconSource="{Binding ApplicationIcon, Mode=OneWay}"
		ToolTipText="Double-click for window, right-click for menu"
	>
		<Taskbar:TaskbarIcon.ContextMenu>
			<ContextMenu >
				<ContextMenu.Resources>
					<SC:LogicalNotConverter x:Key="LogicalNotConverter"/>

					<ItemContainerTemplate x:Key="NoteMenuItemTemplate">
						<StackPanel Orientation="Horizontal">
							<Image Source="{Binding MenuImage}" />
							<TextBlock Text="{Binding DisplayText}" />
						</StackPanel>
					</ItemContainerTemplate>
					<Style x:Key="NoteMenuItemStyle" TargetType="MenuItem">
						<Setter Property="Command" Value="{Binding Path=PlacementTarget.MenuSelectedCommand,  RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContextMenu}}}" />
					</Style>
				</ContextMenu.Resources>
				<MenuItem AA:Language.Word="tasktray/setting" Header="setting" Command="{Binding ShowSettingWindowCommand}" />
				<MenuItem AA:Language.Word="tasktray/toolbar" Header="toolbar" ItemsSource="{Binding LauncherToolbar}">
					<MenuItem.ItemContainerStyle>
						<Style TargetType="MenuItem">
							<Setter Property="IsChecked" Value="{Binding IsVisible, Mode=OneWay}"/>
							<Setter Property="Command" Value="{Binding ChangeVisibleCommand}" />
							<Setter Property="CommandParameter" Value="{Binding IsVisible, Converter={StaticResource LogicalNotConverter}}" />
						</Style>
					</MenuItem.ItemContainerStyle>
					<MenuItem.ItemTemplate>
						<DataTemplate>
							<StackPanel Orientation="Horizontal">
								<Image Source="{Binding ScreenPosition}" />
								<TextBlock Text="{Binding ScreenName}" />
							</StackPanel>
						</DataTemplate>
					</MenuItem.ItemTemplate>
				</MenuItem>
				<MenuItem AA:Language.Word="tasktray/note"    Header="note">
					<MenuItem Header="create" Command="{Binding CreateNoteItemCommand}" />
					<MenuItem Header="compact" />
					<MenuItem Header="hide" />
					<Separator />
					<MenuItem Header="front" />
					<Separator />
					<MenuItem Header="show" ItemsSource="{Binding NoteShowItems}" ItemTemplate="{StaticResource ResourceKey=NoteMenuItemTemplate}" ItemContainerStyle="{StaticResource ResourceKey=NoteMenuItemStyle}" />
					<MenuItem Header="hidden" ItemsSource="{Binding NoteHiddenItems}" ItemTemplate="{StaticResource ResourceKey=NoteMenuItemTemplate}" ItemContainerStyle="{StaticResource ResourceKey=NoteMenuItemStyle}" Command="{Binding MenuSelectedCommand}" />
				</MenuItem>
				<MenuItem AA:Language.Word="tasktray/command" Header="command" />
				<MenuItem AA:Language.Word="tasktray/log"     Header="log" Command="{Binding SwitchLoggingWindowCommand}" IsChecked="{Binding Logging.IsVisible, Mode=OneWay}" />
				<Separator />
				<MenuItem AA:Language.Word="tasktray/log" Header="system-env" />
				<Separator />
				<MenuItem AA:Language.Word="tasktray/about" Header="about" />
				<MenuItem AA:Language.Word="tasktray/exit" Header="exit" Command="{Binding ExitApplicationCommand}" />
			</ContextMenu>
		</Taskbar:TaskbarIcon.ContextMenu>
		<Taskbar:TaskbarIcon.DataContext>
			<AV:MainWorkerViewModel />
		</Taskbar:TaskbarIcon.DataContext>
	</Taskbar:TaskbarIcon>-->
</ResourceDictionary>