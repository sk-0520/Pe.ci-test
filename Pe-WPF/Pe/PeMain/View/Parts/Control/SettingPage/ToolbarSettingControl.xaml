<CD:CommonDataUserControl 
	x:Class="ContentTypeTextNet.Pe.PeMain.View.Parts.Control.SettingPage.ToolbarSettingControl"
	xmlns:CD="clr-namespace:ContentTypeTextNet.Pe.PeMain.View.Parts.Control"
	xmlns:UI="clr-namespace:ContentTypeTextNet.Pe.PeMain.View.Parts"
	xmlns:PC="clr-namespace:ContentTypeTextNet.Pe.PeMain.View.Parts.Converter"
	xmlns:SC="clr-namespace:ContentTypeTextNet.Library.SharedLibrary.View.Converter;assembly=SharedLibrary"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	mc:Ignorable="d" 
	d:DesignHeight="318" d:DesignWidth="517"
>
	<CD:CommonDataUserControl.Resources>
		<SC:LogicalIsNotNullConverter x:Key="LogicalIsNotNullConverter" />
		<PC:LauncherGroupItemDisplayTextConverter x:Key="LauncherGroupItemDisplayTextConverter" />
		<PC:SelectedNodeAndLauncherItemConverter x:Key="SelectedNodeAndLauncherItemConverter" />
	</CD:CommonDataUserControl.Resources>
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="200"/>
			<ColumnDefinition Width="198*"/>
			<ColumnDefinition Width="119*"/>
		</Grid.ColumnDefinitions>
		<GroupBox HorizontalAlignment="Stretch">
			<GroupBox.Header>
				<Grid HorizontalAlignment="Stretch" Width="180">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*"/>
						<ColumnDefinition Width="Auto" MinWidth="42" />
					</Grid.ColumnDefinitions>
					<ComboBox Grid.Column="0" ItemsSource="{Binding ToolbarItemList, Mode=OneTime}" DisplayMemberPath="DisplayText" SelectedIndex="0" x:Name="Toolbar" />
					<Button Grid.Column="1" Content="screen" />
				</Grid>
			</GroupBox.Header>
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="95*"/>
					<ColumnDefinition Width="91*"/>
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="24*"/>
					<RowDefinition Height="32*"/>
					<RowDefinition Height="36*"/>
					<RowDefinition Height="36*"/>
					<RowDefinition Height="37*"/>
					<RowDefinition Height="125*"/>
				</Grid.RowDefinitions>
				<Label Content="group" HorizontalAlignment="Left" Grid.Row="0" VerticalAlignment="Top"/>
				<ComboBox Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Top" ItemsSource="{Binding DefaultGroupList}" >
					<ComboBox.ItemTemplate>
						<ItemContainerTemplate>
							<ContentPresenter  Content="{Binding Converter={StaticResource LauncherGroupItemDisplayTextConverter}}" />
						</ItemContainerTemplate>
					</ComboBox.ItemTemplate>
				</ComboBox>

				<Label Content="dock" HorizontalAlignment="Left" Grid.Row="1" VerticalAlignment="Top" />
				<ComboBox Grid.Column="1" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Top" ItemsSource="{Binding Source={x:Static UI:EnumList.DockTypeList}}" SelectedItem="{Binding SelectedItem.DockType, ElementName=Toolbar, Mode=TwoWay}" />

				<Label Content="icon" HorizontalAlignment="Left" Grid.Row="2" VerticalAlignment="Top"/>
				<ComboBox Grid.Column="1" Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Top" ItemsSource="{Binding Source={x:Static UI:EnumList.PlainIconScaleList}}" SelectedItem="{Binding SelectedItem.IconScale, ElementName=Toolbar, Mode=TwoWay}" />

				<Label Content="font" Grid.Row="3" HorizontalAlignment="Stretch" VerticalAlignment="Top" />

				<Label Content="text length" Grid.Row="4" HorizontalAlignment="Stretch" VerticalAlignment="Top" />
				<!-- 記号定数化 -->
				<Slider Grid.Column="1" Grid.Row="4" HorizontalAlignment="Stretch" VerticalAlignment="Top" Value="{Binding SelectedItem.TextWidth, ElementName=Toolbar, Mode=TwoWay}" Minimum="1" Maximum="200" />

				<StackPanel HorizontalAlignment="Left" Height="71" Grid.Row="5" Grid.ColumnSpan="2" VerticalAlignment="Top">
					<CheckBox Content="show text" HorizontalAlignment="Stretch" IsChecked="{Binding SelectedItem.TextVisible, ElementName=Toolbar}" />
					<CheckBox Content="auto hide" HorizontalAlignment="Stretch" IsChecked="{Binding SelectedItem.AutoHide, ElementName=Toolbar}" />
					<CheckBox Content="topmost" HorizontalAlignment="Stretch" IsChecked="{Binding SelectedItem.TopMost, ElementName=Toolbar}" />
					<CheckBox Content="visible" HorizontalAlignment="Stretch" IsChecked="{Binding SelectedItem.Visible, ElementName=Toolbar}" />
				</StackPanel>
			</Grid>
		</GroupBox>
		<Grid Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
			<Grid.RowDefinitions>
				<RowDefinition Height="24"/>
				<RowDefinition/>
			</Grid.RowDefinitions>
			<ToolBarTray HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
				<ToolBar HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
					<StackPanel Orientation="Horizontal">
						<Button Content="group" Command="{Binding CreateGroupCommand}" />
						<Button 
							Content="item" 
							IsEnabled="{Binding SelectedLauncherViewModel, ElementName=ItemList, Converter={StaticResource LogicalIsNotNullConverter}}" 
							Command="{Binding CreateLauncherItemCommand}" 
						>
							<Button.CommandParameter>
								<MultiBinding Converter="{StaticResource SelectedNodeAndLauncherItemConverter}">
									<Binding Path="SelectedItem" ElementName="GroupTree" />
									<Binding Path="SelectedLauncherViewModel" ElementName="ItemList" />
								</MultiBinding>
							</Button.CommandParameter>
						</Button>
						
						<Button Content="up" Command="{Binding NodeUpCommand}" CommandParameter="{Binding SelectedItem,ElementName=GroupTree}" />
						<Button Content="down" Command="{Binding NodeDownCommand}" CommandParameter="{Binding SelectedItem,ElementName=GroupTree}" />
						<Button Content="remove" Command="{Binding NodeRemoveCommand}" CommandParameter="{Binding SelectedItem,ElementName=GroupTree}"  />
					</StackPanel>
				</ToolBar>
			</ToolBarTray>
			<TreeView Grid.Row="1" ItemsSource="{Binding GroupTree}" x:Name="GroupTree" >
				<TreeView.ItemTemplate>
					<HierarchicalDataTemplate ItemsSource="{Binding Nodes}" >
						<StackPanel>
							<Label Content="{Binding DisplayText}" />
						</StackPanel>
					</HierarchicalDataTemplate>
				</TreeView.ItemTemplate>
				<!--<ContentPresenter  Content="{Binding Converter={StaticResource LauncherGroupItemDisplayTextConverter}}" />-->
				<TreeView.ItemContainerStyle>
					<Style TargetType="{x:Type TreeViewItem}">
						<Setter Property="IsExpanded" Value="{Binding IsExpanded}"/>
						<Setter Property="IsSelected" Value="{Binding IsSelected}" />
					</Style>
				</TreeView.ItemContainerStyle>
			</TreeView>
		</Grid>
		<CD:LauncherItemsListControl Grid.Column="2"  DataContext="{Binding LauncherItems}" x:Name="ItemList"/>
	</Grid>
</CD:CommonDataUserControl>
