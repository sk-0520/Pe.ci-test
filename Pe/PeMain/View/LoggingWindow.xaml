<AW:ViewModelCommonDataWindow
  x:Class="ContentTypeTextNet.Pe.PeMain.View.LoggingWindow"
  x:TypeArguments="AVM:LoggingViewModel"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
  xmlns:AVM="clr-namespace:ContentTypeTextNet.Pe.PeMain.ViewModel"
  xmlns:SD="clr-namespace:ContentTypeTextNet.Library.SharedLibrary.Define;assembly=ContentTypeTextNet.SharedLibrary"
  xmlns:AM="clr-namespace:ContentTypeTextNet.Pe.PeMain"
  xmlns:AP="clr-namespace:ContentTypeTextNet.Pe.PeMain.View.Parts"
  xmlns:AW="clr-namespace:ContentTypeTextNet.Pe.PeMain.View.Parts.Window"
  xmlns:AA="clr-namespace:ContentTypeTextNet.Pe.PeMain.View.Parts.Attached"
  xmlns:AC="clr-namespace:ContentTypeTextNet.Pe.PeMain.View.Parts.Converter"
  AA:Language.Word="window/logging"
  Title="LoggingWindow" 
  ShowInTaskbar="False" 
  WindowStyle="ToolWindow"
  ResizeMode="CanResizeWithGrip"
  Visibility="{Binding Visibility}"
  WindowState="{Binding WindowState}"
  Left="{Binding WindowLeft, Mode=TwoWay}"
  Top="{Binding WindowTop, Mode=TwoWay}"
  Width="{Binding WindowWidth, Mode=TwoWay}"
  Height="{Binding WindowHeight, Mode=TwoWay}"
  Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
  Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
  BorderBrush="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}" 
  
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
  d:DesignWidth="300" d:DesignHeight="400" 
>
  <AW:CommonDataWindow.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/View/Parts/Resource/ToolbarDictionary.xaml" />
        <ResourceDictionary Source="/View/Parts/Resource/TextBoxDictionary.xaml"/>
        <ResourceDictionary Source="/View/Parts/Resource/ImageDictionary.xaml"/>
        <ResourceDictionary Source="/View/Parts/Resource/GridSplitterDictionary.xaml"/>
        <ResourceDictionary Source="/View/Parts/Resource/LabelDictionary.xaml"/>
      </ResourceDictionary.MergedDictionaries>
      <AC:SelectedIndexNumberConverter x:Key="SelectedIndexNumberConverter" />
      <AC:LogFileNameConverter x:Key="LogFileNameConverter" />
      <AC:BooleanToTextWrappingConverter x:Key="BooleanToTextWrappingConverter" />
    </ResourceDictionary>
  </AW:CommonDataWindow.Resources>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <ToolBar Grid.Row="0" Height="Auto" Style="{StaticResource DefaultToolbar}" FontFamily="{DynamicResource {x:Static SystemFonts.MenuFontFamilyKey}}" FontSize="{DynamicResource {x:Static SystemFonts.MenuFontSizeKey}}" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" >
      <Button Command="{Binding SaveCommand}">
        <StackPanel Orientation="Horizontal">
          <Image Source="{x:Static AM:AppResource.CommonSaveImage}" Style="{StaticResource ItemImage}" />
          <Label Content="save" AA:Language.Word="logging/save" Style="{StaticResource ContentTextLabel}" />
        </StackPanel>
      </Button>
      <Button Command="{Binding ClearCommand}">
        <StackPanel Orientation="Horizontal">
          <Image Source="{x:Static AM:AppResource.CommonClearImage}" Style="{StaticResource ItemImage}" />
          <Label Content="clear" AA:Language.Word="logging/clear" Style="{StaticResource ContentTextLabel}" />
        </StackPanel>
      </Button>
      <Separator />
      <ToggleButton IsChecked="{Binding AttachmentOutputLogging}">
        <StackPanel Orientation="Horizontal">
          <Image Source="{x:Static AM:AppResource.LogAttachmentImage}" Style="{StaticResource ItemImage}" />
          <Label Content="attachment" AA:Language.Word="logging/attachment" Style="{StaticResource ContentTextLabel}" />
        </StackPanel>
      </ToggleButton>
    </ToolBar>
    <Grid Grid.Row="1">
      <Grid.RowDefinitions>
        <RowDefinition Height="154*"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="103*"/>
      </Grid.RowDefinitions>
      <ListView Grid.Row="0" ItemsSource="{Binding LogItems}" x:Name="listLog" SelectedItem="{Binding SelectedItem}">
        <ListView.View>
          <GridView>
            <GridViewColumn Header="timestamp" AA:Language.Word="logging/timestamp">
              <GridViewColumn.CellTemplate>
                <DataTemplate>
                  <StackPanel Orientation="Horizontal">
                    <Image AA:IconSize.IconScale="Small">
                      <Image.Style>
                        <Style TargetType="Image" BasedOn="{StaticResource DefaultImage}">
                          <Style.Triggers>
                            <DataTrigger Binding="{Binding LogKind}" Value="{x:Static SD:LogKind.Debug}">
                              <Setter Property="Source" Value="{x:Static AM:AppResource.LogDebugImage}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding LogKind}" Value="{x:Static SD:LogKind.Trace}">
                              <Setter Property="Source" Value="{x:Static AM:AppResource.LogTraceImage}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding LogKind}" Value="{x:Static SD:LogKind.Information}">
                              <Setter Property="Source" Value="{x:Static AM:AppResource.LogInformationImage}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding LogKind}" Value="{x:Static SD:LogKind.Warning}">
                              <Setter Property="Source" Value="{x:Static AM:AppResource.LogWarningImage}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding LogKind}" Value="{x:Static SD:LogKind.Error}">
                              <Setter Property="Source" Value="{x:Static AM:AppResource.LogErrorImage}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding LogKind}" Value="{x:Static SD:LogKind.Fatal}">
                              <Setter Property="Source" Value="{x:Static AM:AppResource.LogFatalImage}" />
                            </DataTrigger>
                          </Style.Triggers>
                        </Style>
                      </Image.Style>
                    </Image>
                    <TextBlock Text="{Binding Timestamp}" />
                  </StackPanel>
                </DataTemplate>
              </GridViewColumn.CellTemplate>
            </GridViewColumn>
            <GridViewColumn Header="message" AA:Language.Word="logging/message" DisplayMemberBinding="{Binding Message}" />
          </GridView>
        </ListView.View>
      </ListView>
      <GridSplitter Grid.Row="1" Style="{StaticResource DefaultHorizonGridSplitter}" />
      <Grid Grid.Row="2">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ToolBarTray Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Orientation="Vertical">
          <ToolBar Style="{StaticResource DefaultToolbar}" VerticalAlignment="Stretch" VerticalContentAlignment="Stretch" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
            <Button AA:Language.Hint="logging/item/save" Command="{Binding ItemSaveCommand}">
              <Image Source="{x:Static AM:AppResource.CommonSaveImage}" Style="{StaticResource DefaultImage}" />
            </Button>
            <Button AA:Language.Hint="logging/item/copy" Command="{Binding ItemCopyCommand}">
              <Image Source="{x:Static AM:AppResource.CommonCopyImage}" Style="{StaticResource DefaultImage}" />
            </Button>
            <Button AA:Language.Hint="logging/item/clear" Command="{Binding ItemRemoveCommand}">
              <Image Source="{x:Static AM:AppResource.CommonRemoveImage}" Style="{StaticResource DefaultImage}" />
            </Button>
            <Separator />
            <ToggleButton AA:Language.Hint="logging/ward-wrap" IsChecked="{Binding DetailWordWrap}">
              <Image Source="{x:Static AM:AppResource.CommonWordWrapImage}" Style="{StaticResource DefaultImage}" />
            </ToggleButton>
          </ToolBar>
        </ToolBarTray>
        <Grid Grid.Row="0" Grid.Column="1">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="caller" AA:Language.Word="logging/detail/caller" Style="{StaticResource TitleLabel}" />
            <Label Grid.Row="0" Grid.Column="1" Content="member" AA:Language.Word="logging/detail/caller/member" Style="{StaticResource HeaderLabel}" />
            <xctk:AutoSelectTextBox Grid.Row="0" Grid.Column="2" AutoSelectBehavior="OnFocus" IsReadOnly="True" Text="{Binding SelectedItem.CallerMember, ElementName=listLog, Mode=OneWay}" Style="{StaticResource DefaultTextBox}" />

            <Label Grid.Row="1" Grid.Column="1" Content="file" AA:Language.Word="logging/detail/caller/file" Style="{StaticResource HeaderLabel}" />
            <Grid Grid.Row="1" Grid.Column="2" >
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <xctk:AutoSelectTextBox Grid.Column="0" AutoSelectBehavior="OnFocus" IsReadOnly="True" Text="{Binding SelectedItem.CallerFile, ElementName=listLog, Mode=OneWay, Converter={StaticResource LogFileNameConverter}}" Style="{StaticResource DefaultTextBox}" />
              <xctk:AutoSelectTextBox Grid.Column="1" AutoSelectBehavior="OnFocus" IsReadOnly="True" AA:Language.Hint="logging/detail/caller/number" Text="{Binding SelectedItem.CallerLine, ElementName=listLog, Mode=OneWay}" Style="{StaticResource DefaultTextBox}" />
            </Grid>
          </Grid>
        </Grid>
        <TextBox
          Grid.Row="1"
          Grid.Column="1"
          AllowDrop="False"
          HorizontalScrollBarVisibility="Auto"
          VerticalScrollBarVisibility="Auto"
          TextWrapping="{Binding DetailWordWrap, Mode=OneWay, Converter={StaticResource BooleanToTextWrappingConverter}}"
          IsUndoEnabled="False"
          IsReadOnly="True" 
          Text="{Binding SelectedValue.DetailText, ElementName=listLog, Mode=OneWay}"
          Style="{StaticResource DefaultTextBox}"
        />
      </Grid>
    </Grid>
    <StatusBar
      Grid.Row="2" 
      FontFamily="{DynamicResource {x:Static SystemFonts.StatusFontFamilyKey}}"
      FontSize="{DynamicResource {x:Static SystemFonts.StatusFontSizeKey}}"
      Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
      Height="{DynamicResource {x:Static SystemParameters.MenuHeightKey}}"
    >
      <TextBlock>
        <Run Text="{Binding SelectedIndex, ElementName=listLog, Converter={StaticResource SelectedIndexNumberConverter}, Mode=OneWay}" />
        <Run>/</Run>
        <Run Text="{Binding LogItems.Count, Mode=OneWay}" />
      </TextBlock>
    </StatusBar>
  </Grid>
</AW:ViewModelCommonDataWindow>
