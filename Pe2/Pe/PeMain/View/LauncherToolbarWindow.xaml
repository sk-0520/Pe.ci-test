<W:ViewModelCommonDataWindow
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:VM="clr-namespace:ContentTypeTextNet.Pe.PeMain.ViewModel"
	xmlns:UI="clr-namespace:ContentTypeTextNet.Pe.PeMain.View.Parts"
	xmlns:W="clr-namespace:ContentTypeTextNet.Pe.PeMain.View.Parts.Window"
	xmlns:Control="clr-namespace:ContentTypeTextNet.Library.SharedLibrary.View.Control;assembly=SharedLibrary"
	x:Class="ContentTypeTextNet.Pe.PeMain.View.LauncherToolbarWindow"
	x:TypeArguments="VM:LauncherToolbarViewModel"
	UI:XamlTag.LanguageKey="window/toolbar"
	Title="ToolbarWindow" 
	Height="300" 
	Width="300"
	WindowStyle="None"
	ShowInTaskbar="True" 
	ResizeMode="NoResize"
	ScrollViewer.VerticalScrollBarVisibility="Disabled"
	WindowStartupLocation="Manual"
	Visibility="{Binding Visibility}" 
	Topmost="{Binding TopMost}"
>
	<ToolBarTray Orientation="{Binding Orientation, Mode=OneWay}" ClipToBounds="True">
		<ToolBar>
			<Button>
				<StackPanel Orientation="Horizontal">
					<Image />
					<TextBlock Text="Rotate"/>
					<Path x:Name="BtnArrow" Margin="4" VerticalAlignment="Center" Width="6" Fill="#FF527DB5" Stretch="Uniform" HorizontalAlignment="Right" Data="F1 M 301.14,-189.041L 311.57,-189.041L 306.355,-182.942L 301.14,-189.041 Z "/>
				</StackPanel>
			</Button>
			<Menu  Name="rotAnimMenu"  Visibility="Hidden"  HorizontalAlignment="Left" Margin="374,114,0,0"  VerticalAlignment="Top"  >
				<MenuItem >
					<MenuItem.Header>
						<DockPanel>
							<Image Height="32" ></Image>
							<Label>Rotate 90°</Label>
						</DockPanel>
					</MenuItem.Header>

				</MenuItem>
				<MenuItem >
					<MenuItem.Header>
						<DockPanel>
							<Image Height="32" ></Image>
							<Label>Rotate 180°</Label>
						</DockPanel>
					</MenuItem.Header>

				</MenuItem>
			</Menu>
		</ToolBar>
		<Control:TemplateBindingToolBar ItemsSource="{Binding LauncherItems}">
			<Control:TemplateBindingToolBar.Resources>
				<Style x:Key="Image" TargetType="{x:Type Image}">
					<Setter Property="Source" Value="{Binding ToolbarImage, Mode=OneWay}" />
				</Style>
				<Style x:Key="Text" TargetType="{x:Type TextBlock}">
					<Setter Property="Text" Value="{Binding ToolbarText, Mode=OneWay}" />
				</Style>
			</Control:TemplateBindingToolBar.Resources>
			<Control:TemplateBindingToolBar.ItemTemplate>
				<DataTemplate>
					<StackPanel Orientation="Vertical">

						<!-- ファイル -->
						<Button Visibility="{Binding VisibilityFile, Mode=OneWay}">
							<StackPanel Orientation="Horizontal">
								<Image Style="{StaticResource Image}"/>
								<TextBlock Style="{StaticResource Text}" />
							</StackPanel>
						</Button>
						<!-- ディレクトリ -->
						<Button Visibility="{Binding VisibilityDirectory, Mode=OneWay}">
							<StackPanel Orientation="Horizontal">
								<Image Style="{StaticResource Image}"/>
								<TextBlock Style="{StaticResource Text}" />
							</StackPanel>
						</Button>
						<!-- コマンド -->
						<Button Visibility="{Binding VisibilityCommand, Mode=OneWay}">
							<StackPanel Orientation="Horizontal">
								<Image Style="{StaticResource Image}"/>
								<TextBlock Style="{StaticResource Text}" />
							</StackPanel>
						</Button>
					</StackPanel>
				</DataTemplate>
			</Control:TemplateBindingToolBar.ItemTemplate>
		</Control:TemplateBindingToolBar>
	</ToolBarTray>
	<!--</Grid>-->
</W:ViewModelCommonDataWindow>
