<ResourceDictionary
	x:Class="ContentTypeTextNet.Pe.PeMain.View.MainWorker"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:Taskbar="http://www.hardcodet.net/taskbar"
	xmlns:local="clr-namespace:ContentTypeTextNet.Pe.PeMain.ViewModel"
>

	<ContextMenu x:Shared="false" x:Key="TasktrayContextMenu">
		<MenuItem Header="setting" Tag="lang:setting" Command="{Binding ShowSettingWindowCommand}" />
		<MenuItem Header="toolbar" Tag="lang:toolbar"/>
		<MenuItem Header="note" Tag="lang:note"/>
		<MenuItem Header="command" Tag="lang:command"/>
		<Separator />
		<MenuItem Header="system-env" Tag="lang:system-env"/>
		<Separator />
		<MenuItem Header="about" Tag="lang:about"/>
		<MenuItem Header="exit" Tag="lang:exit"  Command="{Binding ExitApplicationCommand}" />
	</ContextMenu>


	<!-- the application's NotifyIcon - started from App.xaml.cs. Declares its own view model. -->
	<Taskbar:TaskbarIcon 
		x:Key="NotifyIcon"
		IconSource="/Resources/Icon/App.ico"
		ToolTipText="Double-click for window, right-click for menu"
		ContextMenu="{StaticResource TasktrayContextMenu}"
	/>
</ResourceDictionary>