<Window
  x:Class="AvalonEditSyntax.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

  xmlns:sharpdevelop_avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"

  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

  Title="MainWindow"
  Width="800"
  Height="450"
  
  mc:Ignorable="d"
>
  <Window.Resources>
    <Style TargetType="sharpdevelop_avalonedit:TextEditor">
      <Setter Property="ShowLineNumbers" Value="True" />
      <Setter Property="Options">
        <Setter.Value>
          <sharpdevelop_avalonedit:TextEditorOptions
            ShowEndOfLine="True"
            ShowSpaces="True"
            ShowTabs="True"
            HighlightCurrentLine="True"
          />
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <sharpdevelop_avalonedit:TextEditor x:Name="editPreview" Grid.Column="0" />

    <GridSplitter ResizeDirection="Columns" Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />
    <Button Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Content="←反映" Click="Button_Click" />

    <Grid Grid.Column="2">
      <Grid.RowDefinitions>
        <RowDefinition Height="8*" />
        <RowDefinition Height="2*" />
      </Grid.RowDefinitions>
      
      <sharpdevelop_avalonedit:TextEditor x:Name="editSyntax" Grid.Row="0" SyntaxHighlighting="XML" />
      <TextBlock x:Name="showError" Grid.Row="1" />
    </Grid>

  </Grid>
</Window>
