import { Box } from "@mui/material";
import type { FC } from "react";
import type { PageProps } from "../../types/PageProps";

export const HelpHistoryPage: FC<PageProps> = (props: PageProps) => {
	return <Box>気が向いたらやるよ！</Box>;
};
