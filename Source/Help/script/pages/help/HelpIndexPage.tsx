import type { FC } from "react";
import type { PageProps } from "../../types/PageProps";

export const HelpIndexPage: FC<PageProps> = (
	props: PageProps,
) => {
	return <>{markdown}</>;
};

const markdown = `
# asda
`
