import { Box } from "@mui/material";
import type { FC } from "react";
import type { PageProps } from "../../types/page";

export const HelpSearchPage: FC<PageProps> = (props: PageProps) => {
	return <Box>さいごにやるよ！</Box>;
};
