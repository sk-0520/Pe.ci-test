<UserControl
  x:Name="root"
  x:Class="ContentTypeTextNet.Pe.Core.Views.NumericUpDown"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  mc:Ignorable="d"
  d:DesignHeight="450"
  d:DesignWidth="800"
>
  <Grid>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition />
        <RowDefinition />
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition />
        <ColumnDefinition />
      </Grid.ColumnDefinitions>

      <TextBox
        x:Name="PART_NUMERIC"
        Grid.Row="0"
        Grid.RowSpan="2"
        Grid.Column="0"
        Text="{Binding Value, ElementName=root}"
        MouseWheel="PART_NUMERIC_MouseWheel"
      />

      <RepeatButton x:Name="PART_UP_BUTTON" Grid.Row="0" Grid.Column="1" Click="PART_UP_BUTTON_Click">
      </RepeatButton>
      <RepeatButton x:Name="PART_DOWN_BUTTON" Grid.Row="1" Grid.Column="1" Click="PART_DOWN_BUTTON_Click">
      </RepeatButton>
    </Grid>

  </Grid>
</UserControl>
