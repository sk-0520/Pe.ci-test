<ResourceDictionary
  x:Class="ContentTypeTextNet.Pe.Core.Views.UpDownSliderDictionary"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
>
  <Style x:Key="UpDownSlider" TargetType="Slider">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Slider">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition />
              <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
              <ColumnDefinition />
              <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <TextBlock
              x:Name="PART_NUMERIC"
              Grid.Row="0"
              Grid.RowSpan="2"
              Grid.Column="0"
              Text="{TemplateBinding Property=Value}"
            />

            <Button x:Name="PART_UP_BUTTON" Grid.Row="0" Grid.Column="1" />
            <Button x:Name="PART_DOWN_BUTTON" Grid.Row="1" Grid.Column="1" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>
