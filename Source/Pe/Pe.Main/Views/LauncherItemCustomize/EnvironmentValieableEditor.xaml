<UserControl
  x:Name="root"
  x:Class="ContentTypeTextNet.Pe.Main.Views.LauncherItemCustomize.EnvironmentValieableEditor"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

  xmlns:sharpdevelop_avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"

  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  mc:Ignorable="d"
  d:DesignWidth="800"
  d:DesignHeight="450"
>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition />
      <ColumnDefinition />
    </Grid.ColumnDefinitions>

    <Label Grid.Row="0" Grid.Column="0" Content="merge" />
    <sharpdevelop_avalonedit:TextEditor x:Name="envMergeEditor" Style="{StaticResource SettingEditor}" Grid.Row="1" Grid.Column="0" Document="{Binding MergeTextDocument, ElementName=root}" Loaded="EnvMergeEditor_Loaded" />

    <Label Grid.Row="0" Grid.Column="1" Content="remove" />
    <sharpdevelop_avalonedit:TextEditor x:Name="envRemoveEditor" Style="{StaticResource SettingEditor}"  Grid.Row="1" Grid.Column="1" Document="{Binding RemoveTextDocument, ElementName=root}" Loaded="EnvRemoveEditor_Loaded" />
  </Grid>
</UserControl>
