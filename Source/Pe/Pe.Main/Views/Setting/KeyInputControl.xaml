<UserControl
  x:Name="root"
  x:Class="ContentTypeTextNet.Pe.Main.Views.Setting.KeyInputControl"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

  xmlns:app_main_views_setting="clr-namespace:ContentTypeTextNet.Pe.Main.Views.Setting"
  xmlns:app_main_views_converter="clr-namespace:ContentTypeTextNet.Pe.Main.Views.Converter"

  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  mc:Ignorable="d"
  d:DesignWidth="800"
  d:DesignHeight="450"
>
  <UserControl.Resources>
    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    <app_main_views_converter:KeyToDisplayConverter x:Key="KeyToDisplayConverter" />
  </UserControl.Resources>
  <StackPanel>
    <ComboBox ItemsSource="{Binding KeyItemsSource, ElementName=root}" SelectedItem="{Binding Key, ElementName=root}" >
      <ComboBox.ItemsPanel>
        <ItemsPanelTemplate>
          <VirtualizingStackPanel
            VirtualizingStackPanel.IsVirtualizing="True"
            VirtualizingStackPanel.VirtualizationMode="Recycling"
          />
        </ItemsPanelTemplate>
      </ComboBox.ItemsPanel>
      <ComboBox.ItemTemplate>
        <DataTemplate>
          <TextBlock Text="{Binding Converter={StaticResource KeyToDisplayConverter}}" />
        </DataTemplate>
      </ComboBox.ItemTemplate>
    </ComboBox>
    <Grid Visibility="{Binding IsVisibleModifierKey, ElementName=root, Converter={StaticResource BooleanToVisibilityConverter}}" >
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition />
        <ColumnDefinition />
      </Grid.ColumnDefinitions>

      <Label Grid.Row="0" Grid.Column="0" Content="shift" />
      <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding ModifierKeyItemsSource, ElementName=root}" SelectedItem="{Binding Shift, ElementName=root}" />

      <Label Grid.Row="1" Grid.Column="0" Content="ctrl" />
      <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding ModifierKeyItemsSource, ElementName=root}" SelectedItem="{Binding Control, ElementName=root}" />

      <Label Grid.Row="2" Grid.Column="0" Content="alt" />
      <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding ModifierKeyItemsSource, ElementName=root}" SelectedItem="{Binding Alt, ElementName=root}" />

      <Label Grid.Row="3" Grid.Column="0" Content="super" />
      <ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding ModifierKeyItemsSource, ElementName=root}" SelectedItem="{Binding Super, ElementName=root}" />
    </Grid>
  </StackPanel>
</UserControl>
