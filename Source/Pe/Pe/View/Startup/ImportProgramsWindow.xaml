<Window
  x:Class="ContentTypeTextNet.Pe.Main.View.Startup.ImportProgramsWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

  xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
  xmlns:prism="http://prismlibrary.com/"

  Title="ImportProgramsWindow"
  Width="800"
  Height="450"

  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  >
  <i:Interaction.Triggers>
    <i:EventTrigger EventName="Loaded" >
      <i:InvokeCommandAction Command="{Binding ViewLoadedCommand}" />
    </i:EventTrigger>
  </i:Interaction.Triggers>
  <Grid>

    <ListBox ItemsSource="{Binding ProgramItems}">
      <ListBox.ItemTemplate>
        <DataTemplate>
          <StackPanel>
            <CheckBox IsChecked="{Binding IsImport}" />
            <TextBlock Text="{Binding FileName, Mode=OneTime}" />
          </StackPanel>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>

  </Grid>
</Window>
